name: blank

on:
  push:

jobs:
  sailor:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        pad: ["oven", "folk", "claim", "bud", "violate"]
        shallow: ["bore", "needy", "mask", "even"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar fh
